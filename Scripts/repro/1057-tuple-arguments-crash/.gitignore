# Regenerated on demand by dump-expansion.sh: 7,500 lines carrying this worktree's absolute
# paths, so it is evidence to re-derive rather than to commit. The one expansion the argument
# turns on is quoted in README.md.
macro-expansions.txt
